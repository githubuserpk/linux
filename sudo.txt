#the below command adds ec2-user to the docker group so that you dont have to type sudo every time before running each command
sudo usermod -a -G docker ec2-user

#this command lists the docker images
docker images

#list the available containers
docker ps
